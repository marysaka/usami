#version 440

layout (local_size_x = 1, local_size_y = 1, local_size_z = 1) in;
layout (r32f, binding=0) uniform image1DArray u_image;

void main (void)
{
	imageStore(u_image, ivec2(0, 0), vec4(1.0));
}
